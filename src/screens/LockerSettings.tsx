import {StyleSheet, Text, View} from 'react-native';
const LockerSettings = () => {
  return (
    <View>
      <Text>LockerSettings</Text>
    </View>
  );
};
export default LockerSettings;
const styles = StyleSheet.create({});
