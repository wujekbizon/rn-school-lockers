import LoginLocker from './LoginLocker';
import RegisterLocker from './RegisterLocker';
import AdminDashboard from './AdminDashboard';
import LockerDashboard from './LockerDashboard';

export {LoginLocker, RegisterLocker, AdminDashboard, LockerDashboard};
